<%= form_with(model: thing, local: true) do |form| %>
  <% if thing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(thing.errors.count, "error") %> prohibited this thing from being saved:</h2>

      <ul>
        <% thing.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">    
    <%= form.radio_button :has_specs, true %> 
    <%= form.label :has_specs_true, "Yes" %>
    <%= form.radio_button :has_specs, false %>
    <%= form.label :has_specs_false, "No" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
